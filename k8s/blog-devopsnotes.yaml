apiVersion: v1
kind: Service
metadata:
  name: blog-service
  namespace: blog-prod
spec:
  selector:
    app: blog-devopsnotes
  ports:
    - name: frontend
      protocol: TCP
      port: 80
      targetPort: 3000
    - name: backend
      protocol: TCP
      port: 5000
      targetPort: 5000
